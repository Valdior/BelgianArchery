{% extends 'layout/layout.html.twig' %}

{% block title %}Edit Tournament{% endblock %}

{% block body %}
    <h1>Edit Tournament</h1>

    {{ include('tournament/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tournament_show', {'id': tournament.id, 'slug': tournament.slug}) }}" class="btn btn-light"><i class="fad fa-chevron-left"></i> back to list</a>

    {{ include('tournament/_delete_form.html.twig') }}
{% endblock %}
