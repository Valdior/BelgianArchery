{% extends 'layout/layout.html.twig' %}

{% block title %}Edit Peloton{% endblock %}

{% block body %}
    <h1>Edit Peloton</h1>

    {{ include('peloton/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('peloton_show', {'id_tournament': peloton.tournament.id, 'slug' : peloton.tournament.slug , 'id': peloton.id }) }}">{% trans %}common.back_list{% endtrans %}</a>

    {{ include('peloton/_delete_form.html.twig') }}
{% endblock %}
